# 001: handle_LR_swap を無効化 [完了]

## 問題

`handle_LR_swap = true` により右腕と左腕がほぼ同一座標に重なる。
- 肩39%、肘71%、手首76%のフレームで発生（肩距離0.007m等）

## 原因

スワップアルゴリズムが一部カメラのL/Rラベルを誤って入れ替え、左右の中間点に収束。
- 2Dポーズデータ自体にはL/R混同なし（各カメラで80〜145px分離）

## 対策

`Config.toml` で `handle_LR_swap = false` に設定。

## 結果

全フレームでL/R重なり0件、急反転も0件。
